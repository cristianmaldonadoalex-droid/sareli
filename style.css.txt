*{
    margin:0;
    padding:0;
    box-sizing:border-box;
}

body{
    font-family:'Poppins',sans-serif;
    background:linear-gradient(180deg,#120018,#25003d,#3b0066);
    color:white;
    overflow-x:hidden;
}

.inicio{
    width:100%;
    height:100vh;
    display:flex;
    flex-direction:column;
    justify-content:center;
    align-items:center;
    text-align:center;
    padding:25px;
}

.inicio h1{
    font-family:'Great Vibes',cursive;
    font-size:70px;
    color:#ffb6ff;
    margin:20px 0;
}

.inicio h2{
    color:#ffffff;
    font-size:28px;
}

.inicio p{
    max-width:700px;
    line-height:1.8;
    font-size:18px;
    margin-top:15px;
}

button{
    margin-top:40px;
    padding:15px 40px;
    border:none;
    border-radius:40px;
    background:#b300ff;
    color:white;
    font-size:20px;
    cursor:pointer;
    transition:.3s;
    box-shadow:0 0 25px #b300ff;
}

button:hover{
    transform:scale(1.08);
    background:#d14cff;
}

#principal{
    display:none;
    animation:aparecer 2s;
    padding:50px 20px;
}

@keyframes aparecer{
    from{
        opacity:0;
    }
    to{
        opacity:1;
    }
}

.titulo{
    text-align:center;
    font-size:55px;
    color:#ffc9ff;
    margin-bottom:15px;
}

.fecha{
    text-align:center;
    font-size:22px;
    margin-bottom:30px;
}

#contador{
    text-align:center;
    font-size:35px;
    color:#ffb3ff;
    margin-bottom:50px;
}

.galeria{
    display:grid;
    grid-template-columns:repeat(auto-fit,minmax(220px,1fr));
    gap:20px;
    margin:40px auto;
    max-width:1200px;
}

.galeria img{
    width:100%;
    height:280px;
    object-fit:cover;
    border-radius:20px;
    border:3px solid rgba(255,255,255,.2);
    transition:.5s;
    box-shadow:0 0 25px rgba(255,255,255,.15);
}

.galeria img:hover{
    transform:scale(1.05);
    box-shadow:0 0 35px #d96dff;
}

.carta{
    max-width:900px;
    margin:80px auto;
    padding:40px;
    border-radius:25px;
    background:rgba(255,255,255,.08);
    backdrop-filter:blur(10px);
}

.carta h2{
    text-align:center;
    margin-bottom:30px;
    color:#ffd6ff;
}

.carta p{
    line-height:2;
    font-size:18px;
    text-align:justify;
}

.final{
    margin-top:100px;
    text-align:center;
    padding-bottom:100px;
}

.final h1{
    font-family:'Great Vibes',cursive;
    font-size:60px;
    color:#ffc4ff;
    margin-bottom:20px;
}

.final p{
    font-size:20px;
    line-height:2;
    max-width:900px;
    margin:auto;
}

.final h2{
    margin-top:40px;
    color:#ffb8ff;
    font-size:35px;
}

#estrellas{
    position:fixed;
    width:100%;
    height:100%;
    background-image:radial-gradient(white 1px,transparent 1px);
    background-size:45px 45px;
    opacity:.25;
    animation:mover 70s linear infinite;
    z-index:-2;
}

@keyframes mover{
    from{
        transform:translateY(0);
    }
    to{
        transform:translateY(1200px);
    }
}

#corazones{
    position:fixed;
    width:100%;
    height:100%;
    overflow:hidden;
    pointer-events:none;
    z-index:-1;
}